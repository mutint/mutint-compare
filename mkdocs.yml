# mutint-compare's documentation. This plugin has no entry script of its own, so these pages
# are read through an assembled project's manual -- `./mutint docs` collects them.
#
# The top-level nav heading is how a page declares who it is for. `Using MutInt` and
# `Extending MutInt` are the two the manual merges; anything under another heading lands under
# "About this deployment" named for this component. See mutint_common/docs_manual.py.

site_name: mutint-compare
site_description: The cross-sample mutation table, with its convergent and fixed sets

nav:
  - Using MutInt:
      - Compare: using/compare.md
